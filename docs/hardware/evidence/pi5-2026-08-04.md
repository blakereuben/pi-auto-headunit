# Pi 5 Milestone 1 Evidence — 4 August 2026

No phone serial numbers or raw USB/session payloads are recorded.

## Target

- Board: Raspberry Pi 5 Model B Rev 1.0, 8 GB
- OS: Debian GNU/Linux 13 (Trixie), `aarch64`
- Kernel: `6.18.39+rpt-rpi-2712`
- Root storage: 238 GB ext4 partition on NVMe
- Project path: `/home/blakereuben/pi-auto-headunit`

## Native build and package

- Trixie Rust/Cargo: 1.85.0
- Workspace format/check: pass
- Strict Clippy with warnings denied: pass
- Native workspace tests: pass (23 tests)
- Development package: `aa-headunit-diagnostics_0.1.0-1_arm64.deb`
- Installed package size: approximately 610 KB
- Installed command and udev rule: pass
- Reinstall over the same version: pass
- Remove: pass; installed command removed
- Reinstall after removal: pass; command and preflight restored
- Purge: pass; installed command removed
- Clean reinstall after purge: pass; version `0.1.0-1` installed and preflight restored
- Older-version upgrade: pass from temporary `0.0.9-1` package metadata to `0.1.0-1`
- Locally modified configuration preserved during upgrade: pass
- Package-owned executable/configuration ownership after upgrade: `root:root`, modes `0755`/`0644`
- Original configuration restored after test with matching SHA-256; temporary package files removed
- Runtime dependencies resolved by package metadata: `libc6`, `libgcc-s1`, `libglib2.0-0t64`, `libgstreamer1.0-0`, `libusb-1.0-0`, and the selected GStreamer base/bad/libav plugin packages
- Rebuilt package installation and installed `aa-headunit-diagnostics media probe`: pass
- Lintian: development warnings for AppStream metadata, changelog closure, manual page, and a fortify informational result; no structural package error

## Wireless discovery

- Wi-Fi: onboard `wlan0`, driver `brcmfmac`; initially ready and later intentionally soft-disabled for the independent USB test
- Bluetooth: onboard controller `hci1` while the USB dongle is attached, driver `hci_uart_bcm`, state ready
- Automatic provider selection: onboard Wi-Fi and onboard Bluetooth when both are ready
- USB Wi-Fi: TP-Link Archer T2U Nano, `2357:011e`, RTL8811AU, `rtw88_8821au`, interface `wlan1`
- USB Bluetooth: CSR-compatible dongle, `0a12:0001`, `btusb`, controller `hci0`, Bluetooth 4.0
- Disabled-state detection: pass for both onboard/USB Wi-Fi and the soft-blocked USB Bluetooth controller
- Independent external-radio enable: pass; USB Wi-Fi and USB Bluetooth enabled without enabling onboard Wi-Fi
- Explicit USB/USB provider selection: pass using stable physical identities
- USB Wi-Fi capability advertisement: 2.4/5 GHz, AP/AP-VLAN modes, and `start_ap`; a live Android Auto wireless session is not yet tested
- USB-radio hot-unplug: pass; both external providers disappeared cleanly
- Return to onboard providers: pass; onboard `wlan0` and onboard Bluetooth became ready and were selected automatically

## Display, touch, and audio inventory

- Display connector: DSI connected; both HDMI connectors disconnected
- Display mode: `800x480`
- Touch controller: `generic ft5x06`, registered as touchscreen input `event6`
- Touch coordinate range: X `0..799`, Y `0..479`, matching the native display mode
- Single-touch press, continuous movement, and release: pass
- Two-finger multi-touch: pass; independent slots 0 and 1, tracking IDs, movement, and both releases observed
- Rotation and calibration: not yet tested
- Full-screen moving 800x480 colour-pattern rendering through the active Wayland desktop: pass; user confirmed it was visible on the physical display

## Synthetic H.264 baseline

- Source: generated 60-second, 800x480, 30 fps H.264 test pattern; no third-party media
- Frames: 1,800
- Pi 5 decode path: FFmpeg software H.264 decoder; the Pi exposes hardware HEVC decode but no H.264 decoder device
- Decode result: approximately 1.106 seconds elapsed, `54.3x` real-time
- Decode CPU time: 3.220 seconds user plus 0.155 seconds system
- Peak resident memory: 75,264 KiB
- Full-screen H.264 decode and Wayland presentation: pass; user confirmed smooth visible playback
- Temporary synthetic media removed after the test
- GStreamer path: `h264parse` -> `avdec_h264` -> `videoconvert` -> `waylandsink`
- GStreamer decode result: 60 seconds decoded in approximately 1.409 seconds, about `42.6x` real-time
- GStreamer full-screen Wayland presentation: pass; user confirmed full-screen playback on the physical display
- GStreamer emitted a window-fullscreen assertion during forced timeout shutdown; visual output passed, but application-owned Wayland surface lifecycle remains an integration item
- Selected Pi 5 baseline: software H.264 decode plus Wayland/GPU composition; HEVC hardware remains available only if an approved protocol source later permits HEVC negotiation
- Board-independent `media-api` decoder selection: pass; exact-codec hardware preference, measured software fallback, codec non-substitution, policy rejection, and invalid-mode tests
- Native `media-gstreamer` capability probe: pass; selected `gstreamer:avdec_h264` software decoding for `800x480@30` and verified `h264parse!avdec_h264!videoconvert!waylandsink`
- USB audio: GeneralPlus `1b3f:2008`, ALSA card 2
- Playback: stereo USB Audio endpoint with speaker volume/mute control
- Capture: USB Audio microphone endpoint with microphone and automatic-gain controls
- Audible playback: pass; user confirmed the 440 Hz test tone
- Microphone loopback: fail with this microphone; capture contained static but no intelligible speech
- Native mono capture peaked at approximately `-39.4 dB`; automatic gain improved the measured peak to approximately `-33.4 dB`
- A 20 dB-amplified playback still contained only static; microphone/sound-card electrical compatibility or the microphone itself must be checked before selection
- PipeWire devices: two built-in HDMI controllers plus the USB sound card
- Active/default output: USB Audio Device Analog Stereo
- Active/default input: USB Audio Device Mono
- HDMI connectors: disconnected; no usable HDMI audio sink in the official DSI reference setup
- Pi 5 analogue audio: unavailable; the board has no built-in analogue audio jack
- Reference audio decision: USB audio is the primary Pi 5 bench output until the custom carrier/audio design is selected

## First AOA transition

- Phone vendor: Samsung (`04e8`); exact model not recorded
- Initial enumeration: `04e8:6860`, bus/address `4:4`, physical port path `1`
- AOA protocol version: 2
- Accessory enumeration: `18d1:2d00`, bus/address `4:5`, same physical port path
- Bulk interface: 0
- Bulk IN: `0x81`
- Bulk OUT: `0x01`
- Unprivileged access: pass as user in `plugdev`
- Clean unplug: pass; phone disappeared and USB inventory remained usable

## Active-interface unplug

- Starting accessory: `18d1:2d00`, bus/address `4:6`, physical port path `1`
- Bulk interface held open and claimed by the diagnostic: pass
- Phone physically unplugged during the 30-second controlled hold
- Result: `hold_result=unplug_detected`; process exited normally
- Automated fake-backend coverage injects unplug at every backend-driven AOA state
- Limitation: the physical test covers the actively claimed bulk state; the earlier control transitions complete too quickly for repeatable manual cable removal
- Post-unplug recovery: phone returned as `04e8:6860` at `4:7`, transitioned to `18d1:2d00` at `4:8`, and reached `BulkTransportReady` without a Pi reboot or service reset
- Post-recovery preflight: supported Pi 5/Trixie baseline passed; onboard Wi-Fi and Bluetooth remained ready

## Reconnection

- Reconnected enumeration: `04e8:6860`, bus/address `4:7`, physical port path `1`
- Accessory enumeration: `18d1:2d00`, bus/address `4:8`, same physical port path
- AOA protocol version: 2
- Bulk interface/endpoints unchanged
- Reconnect without Pi reboot or service reset: pass

## Accessory-mode stability soak

- Starting device: `18d1:2d00`, bus/address `4:8`
- Repeated bulk-interface open/claim/release cycles: 100/100 pass
- Open file descriptors: 8 before, 8 after
- Resident memory: 2,288 KiB before, 2,288 KiB after
- Observed handle or memory growth: none

## Still required before broader product validation

- Repeated physical cable connect/disconnect soak (the accessory-mode 100-cycle interface soak passes)
- Pi 4, Waveshare CM4, and CM5 evidence
- Additional phone vendors/Android versions
