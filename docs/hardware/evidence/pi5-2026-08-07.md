# Pi 5 authorised runtime TLS evidence — 7 August 2026

## Scope

The Pi 5 reference system built and installed the generic runtime-credential probe through commit `8298cff`. Credential material was supplied and authorised by the operator outside the repository. Its contents, identity, fingerprint, and provenance were not recorded, copied into the build tree, or included in logs.

The probe was intentionally limited to USB accessory transition, Android Auto protocol version negotiation, and TLS. It stopped before authentication completion, service discovery, channel setup, media, audio, touch, identifiers, or user content.

## Package and protection

- Package: `/home/blakereuben/aa-headunit-diagnostics_0.1.0-1_arm64.deb`
- Architecture: `arm64`
- SHA-256: `3e87606abc522f6975d96e910dbd25d3a36394cc06854de7453afbe5277fff04`
- Credential directory: root-owned, mode `0700`
- Certificate: root-owned, mode `0644`
- Private key: root-owned, mode `0600`
- Credential contents logged: false

Pi-native formatting, strict Clippy, and all 78 workspace tests passed before the package was built.

## Sanitised live result

The freshly connected phone completed these named states:

1. accessory transport prepared;
2. protocol version request sent;
3. protocol version accepted;
4. TLS peer data received;
5. TLS handshake complete;
6. probe stopped before authentication completion and service discovery.

The initial project-generic AOA identity switched the phone into ordinary accessory mode but did not activate the projection listener. The corrected identification retains this project's own URI and development serial while using the compatibility manufacturer, model, description, and version fields required to select Android Auto.

## Interpretation

The earlier error-7 gate is cleared for this operator-authorised external identity: the phone accepted version negotiation and completed TLS. This result does not yet prove authentication completion, service discovery, media negotiation, video, audio, microphone, touch, reconnect, or long-duration stability. Those remain separate bounded milestones.

