# Pi 5 Evidence — 5 August 2026

## Privacy-preserving service discovery

The Pi 5 reference system natively verified the first attributed OpenAuto-derived behaviour and the corresponding AASDK service-discovery schema handling.

- Raspberry Pi target: Pi 5, 64-bit Raspberry Pi OS
- Build mode: native, locked, offline
- Formatting: pass
- Workspace compilation, all targets: pass
- Strict linting with warnings denied: pass
- Workspace tests: 66 passed, 0 failed
- `protocol-aap` tests: 31 passed, including four service-discovery parser tests and four service-catalogue tests
- Test input: deterministic synthetic protobuf data only
- Phone traffic: none
- Authentication material: none added or used

The tested boundary retains only field presence, byte counts, and an unknown-field count. It does not retain or log discovery labels, device names, icon data, nested phone information, or raw payloads.

## Bounded internal service catalogue

The same native gate verified a synthetic catalogue covering microphone, media audio, speech audio, system audio, sensors, video, Bluetooth, and input roles. Only ready services are advertised; disabled or hardware-unavailable roles remain absent. Channel zero, duplicate channels, duplicate roles, and excessive candidate counts fail closed.

No service-discovery response bytes were generated. Inspection found that pinned OpenAuto and pinned maintained AASDK use materially different service response schemas, so wire encoding is deferred until the newer schema is mapped field by field.
