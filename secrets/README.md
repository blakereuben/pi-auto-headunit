# Local credentials

This directory is reserved for credentials that are legitimately issued to the project owner for local development or deployment.

All other files in this directory are ignored by Git. Do not commit certificates, private keys, authentication material, phone identifiers, or captured traffic.

The project does not supply, download, or authorize credentials copied from another head-unit implementation. Runtime configuration must fail safely when an approved credential is unavailable.
